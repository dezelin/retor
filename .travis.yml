sudo: false

language:
  - c++

compiler:
  - clang
  - gcc

before_install:
  - apt-get update -qq
  - apt-get install -qq build-essential
  - apt-get install -qq clang cmake
  - apt-get install -qq libboost-all-dev
  - apt-get install -qq doxygen graphviz

branches:
  only:
    - master
    - devel

script:
  - rm -rf build/
  - mkdir build
  - cd build
  - cmake ..
  - make
  - make doc
  - make test
  - make install
